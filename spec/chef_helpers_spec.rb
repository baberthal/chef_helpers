require 'spec_helper'

describe ChefHelpers do
  it 'has a version number' do
    expect(ChefHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
